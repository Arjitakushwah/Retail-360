name: dbt CI/CD Pipeline

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  dbt_ci_tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: retail_dbt
    env:
      DBT_PROFILES_DIR: ./.dbt
      DBT_TARGET_SCHEMA: ci_${{ github.head_ref || github.sha }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies (dbt-core and Snowflake adapter)
        run: |
          python -m pip install --upgrade pip
          pip install dbt-snowflake

      - name: Configure dbt profiles.yml
        # THIS IS THE FIX: Remove the working-directory override
        # Since defaults.run.working-directory is set to retail_dbt,
        # this step now runs from retail_dbt by default
        run: |
          # 1. Create the .dbt directory (now relative to retail_dbt)
          mkdir -p .dbt
          
          # 2. Write the profiles.yml content
          cat > .dbt/profiles.yml <<EOF
          retail_dbt:
            target: ci
            outputs:
              ci:
                type: snowflake
                account: ${{ secrets.SNOWFLAKE_ACCOUNT }}
                user: ${{ secrets.SNOWFLAKE_USER }}
                password: ${{ secrets.SNOWFLAKE_PASSWORD }}
                role: ${{ secrets.SNOWFLAKE_ROLE }}
                warehouse: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
                database: RETAIL360_DB
                schema: ${{ env.DBT_TARGET_SCHEMA }}
                threads: 4
                client_session_keep_alive: False
          EOF

      - name: 1. dbt debug (Check Connection)
        run: dbt debug --target ci

      - name: 2. dbt run (Compile and Build Models)
        run: dbt run --target ci

      - name: 3. dbt test (Validate Data Quality)
        run: dbt test --target ci

      - name: Cleanup Temporary Schema (Optional but Recommended)
        if: always()
        run: |
          echo "To implement cleanup, add:"
          echo "dbt run-operation drop_schema --args '{schema: ${{ env.DBT_TARGET_SCHEMA }}}'"
